[project]
name = "superstaff-api"
version = "0.1.0"
description = "Backend API for the Superstaff AI employee platform"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
